<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WaspDesktop.Pages.NotificationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Notifications" />
        <Button Grid.Row="1" HorizontalAlignment="Left" Content="Clear" Click="Clear_Click" />

        <ListView x:Name="NotificationList" Grid.Row="2">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border
                        Margin="0,0,0,8"
                        Padding="10"
                        CornerRadius="8"
                        BorderThickness="1"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding TimeText}" />
                                <TextBlock FontWeight="SemiBold" Text="{Binding SeverityText}" />
                                <TextBlock FontWeight="SemiBold" Text="{Binding Title}" />
                            </StackPanel>
                            <TextBlock TextWrapping="Wrap" Text="{Binding Message}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
